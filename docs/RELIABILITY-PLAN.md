# План фиксов и повышения надёжности

Составлен 10 июля 2026 по итогам живых тестов двух параллельных митингов с музыкой.
Приоритет сверху вниз. Каждый пункт: симптом → причина → решение.

## СТАТУС на 11 июля 2026 (что сделано)

Проверено вживую и закоммичено:
- ✅ **Full HD 1080p** (compose SCREEN_GEOMETRY), WSL-лимит 12 ГБ (`.wslconfig`).
  ⚠️ CPU: один 1080p-клиент ≈ 3.6 ГБ RAM + ~4 ядра; два параллельно ≈ 8 ядер при
  6 выделенных → пиково упирается в CPU (см. docs/stability-2026-07-10.md).
- ✅ **Автозаход по ссылке** (P0): планировщик → бот сам входит до live. Включает
  kill-before-launch (P4), толерантность к «Waiting for host» (OCR).
- ✅ **Original sound авто** (P1): setup_meeting_audio, координаты меню под fullscreen
  (78,1040)/(174,909), клик+проверка баннера с ретраями.
- ✅ **Нативный fullscreen** для записи (двойной клик), идемпотентно через OCR.
  ⚠️ enter_fullscreen капризит на связке «зал ожидания/фокус» — иногда сворачивает
  окно (нужна доводка: не входить в fullscreen пока бот в зале ожидания).
- ✅ **Детект внешнего завершения митинга** (вотчер в session.py) — слот освобождается.
- ✅ **Музыка** (mpv) в оба воркера, BotMic, VAD-автопауза.

**P2 модерация чата — ЧТЕНИЕ РАБОТАЕТ (обновлено 11 июля, вечер):**
- ✅ **Блокер OCR снят**: решение — LANCZOS-апскейл скриншота ×3 перед
  распознаванием (прошлые попытки upscale видимо были без LANCZOS/до бинаризации).
  На реальном чате Zoom оба движка ловят мат; **tesseract после апскейла читает
  даже чище PaddleOCR** и почти не ест CPU → он и выбран (CHAT_READER=tesseract).
- ✅ PaddleOCR встроен как опция (CHAT_READER=paddle): paddlepaddle 2.6.2 +
  paddleocr 2.8.1, модели вшиты в образ (/opt/paddleseed, сид копируется в
  ~/.paddleocr — volume zoomprofile заслоняет /home/zoom). ⚠️ paddle-инференс
  дефолтными 10 потоками голодал Zoom-клиент (вылет из митинга) — ограничено
  cpu_threads=2, но под нагрузкой не проверено.
- ✅ Ридер переписан под современный Zoom: чат — встроенная панель (отдельного
  окна «Meeting chat» нет, заголовок панели = имя митинга) → скрин всего экрана
  + кроп правой панели; alt+h — тоггл, жмётся один раз + переоткрытие после 10
  пустых циклов. Склейка paddle-слов в строки по y-боксам.
- ✅ **Живое событие подтверждено end-to-end** (11.07): реальный митинг, сообщение
  «ах ты сука, пошёл нахуй отсюда» → profanity/flagged в ленте.
- ⚠️ **Удаление сообщения реализовано (12.07), ждёт живой проверки**: OCR-ридер
  теперь отдаёт экранные координаты каждой строки (ChatMessage.pos; tesseract
  переведён на TSV-вывод, склейка слов в строки общая с paddle — app/ocrutil.py),
  `gui.delete_chat_message`: правый клик по сообщению → пункт «Delete» ищется
  OCR'ом по экрану (`find_text_on_screen`, фикс-координат нет) → подтверждение
  в диалоге тоже OCR'ом. Офлайн проверено в контейнере (синтетическая панель:
  координаты строки сходятся ±1px, мат ловится). Живой прогон: проверить, что
  правый клик по сообщению открывает меню с Delete и хватает прав хоста.
- ❌ Мьют автора — недостижим, пока OCR не привязывает автора к сообщению
  (sender="чат"); mute_participant остаётся no-op.
- ❌ Исключение окна чата из записи — не сделано (widen Xvfb 2560×1080 +
  `x11grab` регион + парковка чата вне зоны записи).
- ⚠️ Новый баг: окно митинга иногда сворачивается на home (клиент остаётся в
  митинге — на home есть «Return to meeting»), вотчер принимает это за внешнее
  завершение (ложный «митинг завершён извне»). Нужно: вотчеру перед finished
  проверять «Return to meeting»/кликать его; сворачивание — тот же каприз
  fullscreen из бэклога.
- ℹ️ Спам-детектор ловит только флуд/повторы — рекламные одиночные сообщения
  («КУПИ КУРС…») не помечает; при желании добавить словарь спам-паттернов.

## Решение по разрешению: Full HD

Возврат на **1920×1080** для качества записи (720p-запись мыльная). Память больше
не блокер: WSL-лимит поднят до 12 ГБ (`.wslconfig`), два 1080p Zoom-клиента ≈ 6 ГБ.
Плата — перекалибровка координат под 1080p; частично снимается фиксом №1 (отключить
экран preview, тогда автозаход почти не зависит от координат).

---

## P0. Автозаход по ссылке (главная надёжность)

**Симптом:** при входе через планировщик/веб бот застревал на экране пред-входа
(preview) и не входил; статус уходил в `error` (окно митинга не появилось).

**Причины:**
- `dismiss_startup_dialogs` не жал финальный «Join» на preview (частично уже
  исправлено: добавлены `join_meeting`/`click_prejoin_join`, но координата зависит
  от геометрии).
- Если Zoom уже запущен, повторный `zoom --url=zoommtg://` игнорируется — надо
  чистить старый процесс.
- Окно preview иногда выезжает за пределы экрана (см. P3).

**Решение:**
1. **Отключить экран preview глобально** — снять «Always show this preview when
   joining» (или через `zoomus.conf`: `showZoomWebinarUponEntry`/аналог). Тогда
   `zoommtg://` ведёт сразу в митинг/зал ожидания — клик Join не нужен, автозаход
   становится **резолюшн-независимым**. Это ключевой фикс.
2. В `zoom.launch` / `session._run_session` — **kill старого Zoom** перед запуском
   по ссылке (TERM→KILL→wait), чтобы `--url` гарантированно применился.
3. После входа программно вызвать `enable_original_sound()` (см. P2) и убедиться,
   что микрофон = BotMic и бот не замьючен.
4. Улучшить `wait_in_meeting`: помимо размера окна, проверять заголовок/наличие
   тулбара, различать «Waiting Room» (это не ошибка) и «Invalid meeting».

## P1. Original sound слетает при перезапуске Zoom

**Симптом:** после рестарта Zoom-клиента режим микрофона сбрасывается на «Noise
removal (default)», который **режет музыку** → музыки не слышно (подтверждено в
митинге 2).

**Причина:** режим не всегда персистится в профиле (особенно у гостя без аккаунта).

**Решение:** в автозаходе после входа всегда программно включать
`enable_original_sound()`. Проверять по баннеру/меню, что режим применился.

## P2. Модерация чата — чтение и действия

**Симптом:** движок правил (спам + мат RU/EN) работает и оттестирован, но бот
**не читает** реальные сообщения чата и **не применяет** действия.

**Причины:**
- `chatreader.py` — заглушки: `AtspiChatReader.poll()` и `OcrChatReader.poll()`
  возвращают `[]`.
- `gui.mute_participant` / `gui.delete_chat_message` — no-op (не откалибровано).

**Решение:**
1. **Чтение чата:** реализовать один из ридеров. OCR-путь надёжнее под Xvfb:
   открыть панель чата (хоткей), скринить область, tesseract-rus, дедуп новых строк.
   AT-SPI — если Zoom отдаёт дерево доступности (проверить `pyatspi`).
2. **Действия как хост:** откалибровать по скриншотам мьют участника и удаление
   сообщения (Participants → … → Mute; наведение на сообщение → Delete). Требует
   host-прав (бот уже умеет становиться хостом).
3. Включать модерацию в автозаходе, когда у события `moderate=True`.

## P3. Окно митинга больше экрана (тулбар за кадром)

**Симптом:** окно митинга 1188×800 при экране 1280×720 → нижний тулбар (Audio/
Mute/Leave) уходит за нижнюю кромку, клики по нему промахиваются; приходится
двигать окно вверх вручную.

**Причина:** Zoom открывает окно фиксированного размера больше рабочей области.

**Решение:** после входа приводить окно митинга к экрану — `wmctrl`
maximize (`add,maximized_vert,maximized_horz`) или `xdotool windowsize` под
геометрию экрана. На 1080p проблема меньше (800<1080), но привести всё равно.

## P4. Гость не подхватывает zoommtg при живом инстансе

**Симптом:** повторный `zoom --url=` при уже запущенном клиенте открывает домашний
экран, а не митинг (наблюдалось у воркера-2).

**Причина:** single-instance Zoom не всегда роутит новый URL в существующий процесс.

**Решение:** тот же kill-before-launch из P0.2 (единый механизм чистого входа).

## P5. Прочее (мелкое)

- **Cyrillic-имя через xdotool** ненадёжно — уже решено `paste_text` (xclip). Убедиться,
  что автозаход ставит имя через буфер, а не `type`.
- **Координаты облачной записи** (`rec_*`) выверялись под 1080p — при возврате на
  1080p снять пометку `_UNVERIFIED` после проверки.
- **Профиль Zoom в volume** — уже сделано; логин переживает пересоздание.

---

## Порядок работ

1. **Вернуть 1080p** (compose SCREEN_GEOMETRY, `_REF_GEOMETRY`, перекалибровка coords
   логина/аудио под 1080p — значения уже были, вернуть из истории).
2. **P0 автозаход**: отключить preview + kill-before-launch + post-join аудио.
3. **P1 Original sound** в автозаходе.
4. **P3** привести окно к экрану.
5. **Проверка**: создать событие с ссылкой в вебе → бот сам входит до live с музыкой,
   на обоих слотах, в записи 1080p.
6. **P2 модерация чата** (крупный отдельный блок: чтение + действия).
